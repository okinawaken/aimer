spring:
  cloud:
    polaris:
      config:
        groups:
          - name: aimer-console
            files:
              - dynamic-datasource.yaml
              - dynamic-tp.yaml
              - redisson.yaml